#include <stdio.h>

void define_a_quarter(double  

int main()
    {

        double x = 0;
        double y = 0;

        scanf ("%lg %lg", &x, &y);

        
        if (x > 0)
            {
            if (y > 0)
                printf("1");
            else
                printf("4");
            }
        else
            {
            if (y > 0)
                printf("2");
            else
                printf("3");
            }
        return 0;
    }
