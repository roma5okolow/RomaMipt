#include <stdio.h>

int multiply (int x);

int main()
    {

    int x = 0;

    printf("%d", multiply (x));

    return 0;

    }

int multiply (int x)
    {

    scanf("%d", &x);

    x *= (x > 0) ? 5 : 6;

    return x * (x > 0) ? 5 : 6;

    }
