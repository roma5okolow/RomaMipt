#include <stdio.h>

int main()
    {
    const int N = 10000000;

    int i = 0;
    int j = 0;

    int row [N] = {0};

    for (i = 1; row [i-1] != '\n'; i++)
        {
        scanf("%d", row + i -1);

        for(j = 0, j <= i; j++)
            {
            if (

        }


